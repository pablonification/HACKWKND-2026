# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "HACKWKND"

[setup]
script = '''
set -euo pipefail

npm ci

SOURCE_ENV="/Users/macbook/Documents/coding/HACKWKND/.env"

if [ -f "$SOURCE_ENV" ]; then
  if [ ! -f .env ]; then
    cp "$SOURCE_ENV" .env
    echo "✅ .env copied from $SOURCE_ENV"
  else
    echo "ℹ️ .env already exists, skip overwrite"
  fi
else
  echo "❌ Source .env not found at $SOURCE_ENV"
  exit 1
fi

npm run typecheck

'''
